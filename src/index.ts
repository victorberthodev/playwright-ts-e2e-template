// Placeholder so `tsc --noEmit` has inputs while src/ is being scaffolded.
// Real entrypoints live under src/{pages,api,fixtures,data,utils,config}/ — see PROMPT.md.
// This file will be deleted once Stage 3 lands real modules.
export const TEMPLATE_VERSION = '0.1.0';
